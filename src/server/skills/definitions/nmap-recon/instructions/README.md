# Instructions

This skill has no agent-facing prompt yet because no agent invokes skills
(see the platform-level note in `SKILL.md`). When that dispatch layer is
built, its instructions belong here: how an agent should phrase a request to
this skill, what to do with a partial port range, how to interpret
`openPortCount === 0` (closed/filtered, not "safe").
